apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-ddns-controller.fullname" . }}
  labels:
    {{- include "go-ddns-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-ddns-controller.selectorLabels" . | nindent 4 }}
