apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-ddns-controller.fullname" . }}-test-connection"
  labels:
    {{- include "go-ddns-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-ddns-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
